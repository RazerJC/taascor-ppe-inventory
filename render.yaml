databases:
  - name: taascor-ppe-db
    databaseName: taascor_ppe
    plan: free

services:
  - type: web
    name: taascor-ppe-inventory
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: taascor-ppe-db
          property: connectionString
      - key: NODE_ENV
        value: production
